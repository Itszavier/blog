{
  "name": "blog",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "npm run dev --prefix ./frontend & npm start --prefix ./backend & lt --port 5173 --subdomain narrate-client lt --port 8080 --subdomain narrate-server"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@tiptap/extension-placeholder": "^2.4.0"
  }
}
